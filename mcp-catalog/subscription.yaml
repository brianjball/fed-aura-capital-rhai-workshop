apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: mcp-gateway
  namespace: kuadrant-system
spec:
  channel: preview
  installPlanApproval: Manual
  name: mcp-gateway
  source: redhat-operators
  sourceNamespace: openshift-marketplace
  startingCSV: mcp-gateway.v0.7.1
